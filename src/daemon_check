#!/bin/bash

if [ $# -ne 1 ]; then
  echo "Usage: $(basename "$0") <service>"
  exit 1
fi

service="$1"

if ! command -v systemctl >/dev/null 2>&1; then
  echo "systemctl is not available on this system"
  exit 1
fi

exec sudo systemctl status "$service"
